https://launchpad.net/~boost-latest/+archive/ubuntu/ppa
Install boost at least 1.54

#gcc version at least 4.8
sudo apt-get install gcc-4.8 g++-4.8 

sudo apt-get install libboost1.55-all-dev  libevent-dev automake libtool flex bison pkg-config g++ libssl-dev

Install thrift
wget http://mirror.nus.edu.sg/apache/thrift/0.9.2/thrift-0.9.2.tar.gz
./configure --without-java --without-go
make
make install

Remaining dependencies, 14.04
sudo apt-get install libgflags-dev libgoogle-glog-dev liblmdb-dev

Remaining dependencies, 12.04
These dependencies need manual installation in 12.04.
# glog
wget https://google-glog.googlecode.com/files/glog-0.3.3.tar.gz
tar zxvf glog-0.3.3.tar.gz
cd glog-0.3.3
./configure
make && make install
# gflags
wget https://github.com/schuhschuh/gflags/archive/master.zip
unzip master.zip
cd gflags-master
mkdir build && cd build
export CXXFLAGS="-fPIC" && cmake .. && make VERBOSE=1
make && make install

apt-get install libleveldb-dev libsnappy-dev

apt-get install protobuf-compiler  libprotobuf-dev libkrb5-dev libxml2-dev libuuid1 uuid-dev libgsasl7 libgsasl7-dev 

apt-get install libopenmpi-dev openmpi-bin openmpi-common openmpi-doc


vi /etc/ld.so.conf.d/java.conf
/opt/jdk1.7.0_75/jre/lib/amd64
/opt/jdk1.7.0_75/jre/lib/amd64/server


find /usr/lib/hadoop -name *.jar|awk '{ printf("export CLASSPATH=%s:$CLASSPATH\n", $0); }' >> j.sh
find /usr/lib/hadoop/lib -name *.jar|awk '{ printf("export CLASSPATH=%s:$CLASSPATH\n", $0); }' >> j.sh
find /usr/lib/hadoop-hdfs/ -name *.jar|awk '{ printf("export CLASSPATH=%s:$CLASSPATH\n", $0); }' >> j.sh
find /usr/lib/hadoop-hdfs/lib/ -name *.jar|awk '{ printf("export CLASSPATH=%s:$CLASSPATH\n", $0); }' >> j.sh

